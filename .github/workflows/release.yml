name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Create Release
        env:
          RELEASE_ROOT_FOLDER: ${{ github.workspace }}
        run: sh ${{ github.workspace }}/release.sh
        
